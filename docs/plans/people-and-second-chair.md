# Plan: People, second chair, and a fresh book

**Status:** approved direction; progress is in the section 2 status table. Written 2026-09-25 against `031be08`, after Tier 0's code work merged (`docs/plans/tier-0.md`; its remaining items are Jeff's checks on the live site and do not block this plan).
**Why:** the firm's people changed. Six partners (Brendan, Jeff, Joe, Kevin, Mike, Paula), one emeritus (Jay), and associates to come. Steve and Fritz have handed over their clients. Every client needs one partner as lead and may have a second chair, and the scenarios must model an associate work split and the departure of anyone. The app knew none of this: its roster was a hard-coded list of nine names used only by the client form, and every "partner" view was built from whatever name was typed in a client's lead field.
**Audience:** future Claude Code sessions and Jeff. Sessions cannot reach Render or gbacpod.com; steps on production are Jeff's.

---

## 0. How a session uses this document

1. `CLAUDE.md`, then section 1 (decisions) and section 2 (status), then the one phase you are executing.
2. One phase per branch and pull request (Phase 5 is two). Branch from `origin/main` after the previous phase merged.
3. Gates before and after: `npm run lint` (0 errors; 3 `exhaustive-deps` warnings are expected since Phase 4, 7 before), `npm test`, `VITE_API_BASE_URL=https://gbacpod.com npm run build:prod`. Phases that touch `init-db.sql` also run `tests/schema.test.mjs` against a throwaway PostgreSQL (`CLAUDE.md`, Testing).
4. Schema changes go in `init-db.sql`, idempotent, and never drop or rename a column an older `init-db.sql` names: a Render rollback runs the older file at start (`docs/plans/tier-0.md` section 12). Retired columns stay in the table.
5. Unchanged from Tier 0: do not change the scoring weights in `utils/strategic.cjs`, do not upgrade React, Vite or the Anthropic SDK across a major version, and do not start the Tier 1 AI rebuild. Phase 5 changes the transition-plan prompt only as far as section 8 says.
6. Every phase ends with gates green, `CLAUDE.md` updated where behaviour it documents changed, and this file's status table updated in the same PR.
7. If this plan and the code disagree, the code wins; note it in the PR and in section 2.

---

## 1. Decisions

| # | Decision | Rationale |
|---|---|---|
| P1 | People are rows in a `people` table: `name` (unique, case-insensitive), `role` (`partner`, `emeritus`, `associate`), `active`. A new database is seeded with Brendan, Jeff, Joe, Kevin, Mike and Paula as partners and Jay as emeritus, only when the table is empty. Steve, Fritz and Zeke leave the roster; Zeke was Jeff. `src/constants.js` is deleted. | Associates will be hired over time; a constant means a deploy per hire, and the server needs the roster to validate assignments. Seeding only an empty table means a rename or a role change survives every restart. |
| P2 | Roles describe a person's place in the book, not app permissions. The partners are equal in the app: no admin, no permission roles, everyone signed in sees and edits everything. The firm's managing partner is not modelled. Associates do not get logins. | Jeff, 2026-09-25. The book shows every partner's revenue. |
| P3 | Every client has exactly one lead, an active partner, and at most one second chair: any active person other than the lead (partner, emeritus or associate). Stored as `clients.lead_id` and `clients.second_chair_id`, foreign keys to `people`. `lobbyist_team` is retired. | The firm's model is lead plus second chair. Foreign keys stop a typo from becoming a phantom partner and let the server enforce the lead rule. |
| P4 | The originator is a person (any role, active or not) in `clients.originator_id`, plus `clients.originator_is_firm`: ticked when that person's origination credit has ended, or when the firm itself originated the client. Ticking it keeps the person on record. Display: `Kevin`, `Firm (originated by Kevin)`, or `Firm`. | Jeff, 2026-09-25: the originator must be able to toggle to "firm" after the origination credit ends. Who brought a client in stays worth knowing after the credit lapses. |
| P5 | People are never deleted. A person cannot leave the partner role or be deactivated while leading a client, or be deactivated while second chair on one. A departure is modelled on Scenarios, the assignments are changed, then the person is deactivated. | Keeps every client valid under P3 at all times, and keeps the originator history of people who have left. |
| P6 | Until each view is rebuilt, the API keeps returning the legacy fields, filled from the new ones: `primary_lobbyist` = the lead's name, `lobbyist_team` = `[lead, second chair]` or `[lead]` (the succession metrics read its length as the number of people on the client), `client_originator` = `Firm` or the originator's name. A client not yet saved under P3 (no `lead_id`) returns its stored legacy values. Writes also store the legacy text, and a rename rewrites it. | The Partnership tab, the Scenarios workflow, the AI prompts and the exports all read the legacy fields; they keep working lead-only until Phases 4 and 5 replace them. The stored text keeps a Render rollback to pre-plan code showing correct leads. Edits made while rolled back change only the legacy text; after rolling forward, the ids win for any client that has a lead. |
| P7 | The book is started over once: a script deletes every client and revenue row in one transaction after a manual backup, and the new sheet (section 3) is imported. Accounts and people are untouched. | Jeff, 2026-09-25. Leads changed wholesale; restarting also removes the need to migrate multi-name teams to one second chair. |
| P8 | The import sheet (section 3) carries `Lead` and `Second Chair`, and optional originator and judgment columns. As with the revenue years (D5), the file is authoritative for exactly the columns it has: a column the file lacks leaves that field untouched. A file with any people error (unknown name, a non-partner lead, a missing lead, second chair equal to lead) imports nothing and lists every bad row. Added in Phase 2: a value outside a column's vocabulary (section 3's Values column) refuses the file the same way, and so does a `CLIENT` named twice, compared regardless of case. | A partial book is worse than a refused file. |
| P9 | The associate split suggests second-chair assignments and a partner accepts each one; accepting writes the assignment. The brief's "no automated swap optimizer" rule is relaxed for second chairs only. Lead changes are only ever suggested as candidates, during a departure. | Jeff, 2026-09-25. A second chair does not own the relationship, so proposing a balanced set does not risk it. |
| P10 | Load is measured against peers in the same role, as the brief says, not against a fixed ceiling. Lead load and second-chair load are shown separately, each as clients, revenue and effort (`getEffort` in `utils/strategic.cjs`). The lead carries a client's full effort and the second chair 20% of it (`SECOND_CHAIR_EFFORT_SHARE` in `src/utils/load.js`, the one place every second-chair effort figure reads); client counts and revenue count in full for both chairs. **Amended 2026-09-25 (Jeff):** this clause said "no factor splits a client's effort between the two chairs". | Replaces three conflicting capacity formulas (clients ÷ 15, ÷ 30, thresholds 80 and 85). No new number for anyone to negotiate. The amendment, Jeff: "Some are more, some are less, but it is subjective and individual, so that will be a good basic measure." Only load changes: each client's own effort and its strategic score do not. |
| P11 | Scenario state stays in the browser; Phase 5 moves it into the store so a tab switch no longer loses it. Saving scenarios on the server is later work. | Scope. |
| P12 | This plan runs before the Tier 1 AI rebuild. | Tier 1 sends "the whole book with the per-partner table", which depends on the people model. |
| P13 | Contract status is retired. The book holds only active contracts. The `IF`/`P`/`D`/`H` codes derived from `Contract Period` and the form's `Active`/`Prospect`/`Inactive`/`Former` are removed from the API, the import, the form, the views, the exports and the AI prompts. `Contract Period` is no longer read, and a sheet that has it imports as one without it. `clients.status` stays in the table, unread and unwritten (rule 0.4), until a V4 migration drops the retired columns. | Jeff, 2026-09-25: only active contracts belong in the book. Checked against the firm's 9/25/26 sheet, the date-derived status disagreed with the firm's own codes on 36 of 103 clients. Contract tracking, if it returns, is its own design. |

---

## 2. Status table (sessions update this)

| Phase | Title | Status | Branch / PR | Notes |
|---|---|---|---|---|
| 1 | People and second chair | merged | `claude/eager-wozniak-43ogq7`, [PR #19](https://github.com/zekusmaximus/client-portfolio/pull/19) | Section 4's session items pass. Where the plan and the code differed, the code won: (1) `lobbyist_team` is written as `[lead, second chair]`, not `[second chair]`, because `deriveRelationshipType` reads its length as the number of people on the client; (2) the pickers are native `<select>`s (`src/components/ui/native-select.jsx`) because `ui/select.jsx` shows the raw value, an id, in its trigger; (3) found while testing and fixed here because it blocks this phase's workflow: the form's status validation accepted only `Active`/`Prospect`/`Inactive`/`Former`, while every CSV-imported client carries `IF`/`P`/`D`/`H`, so the form could not save any imported client; it now accepts and offers both (merging the vocabularies stays Tier 2); (4) saving a pre-plan client replaces its legacy names with the picks; the form shows the old names first (`Recorded before the People list: ...`) so nothing disappears unseen; (5) not fixed, for Phase 4: `ClientListView` calls `fetchPartners()` inside `useMemo`, a store update during render that React warns about. Jeff's: section 4's last acceptance item after deploy |
| 2 | Import reads people and judgments | merged | `claude/friendly-babbage-laboxa`, [PR #20](https://github.com/zekusmaximus/client-portfolio/pull/20) | Section 5's session items pass. Where the plan and the code differed, the code won: (1) `validateClientData` does not merge the people errors into `issues`: `issues` never blocked the write (a malformed contract period still imports, as before), so the blocking problems are a separate list, `checkSheet` in `utils/csvImport.cjs`, answered as `errors`; (2) names are resolved in `/process-csv`, not `processCSVData`, because a people column the file lacks keeps the stored value and that value is checked too (a new lead who is the kept second chair is refused); (3) `Second Chair`, `Originator` or `Credit To Firm` without a `Lead` column refuses the file (section 3 makes `Lead` required; this way every row that assigns people gets its lead from the same file); (4) the duplicate-`CLIENT` refusal (P8's addition) applies to every file, including one without the new columns, the only change for such a file: before, the last row silently won and the earlier row's revenue was dropped; (5) `CLIENT` and `Contract Period` still match exactly (the request check reads `row.CLIENT`); only the new columns and the years match regardless of case; (6) the request check's messages (`Row N: CLIENT is required`) counted data rows from 1 and now count the header as row 1, like the new list; the page shows them as text instead of raw JSON; (7) a blank `Cadence` clears to `''`, the value the import has always written for none (section 3's clearing list did not name it); (8) found and fixed: the page accepted only `file.type === 'text/csv'`, which browsers on Windows with Excel report as `application/vnd.ms-excel`; it now also accepts a `.csv` name. Added `tests/import-db.test.mjs` (the import on `server.cjs` and PostgreSQL) to CI's `schema` job. Jeff's: section 5's last acceptance item after deploy |
| 3 | Fresh book | deployed | `claude/determined-brahmagupta-kilyyn`, [PR #21](https://github.com/zekusmaximus/client-portfolio/pull/21), follow-ups [PR #22](https://github.com/zekusmaximus/client-portfolio/pull/22) (Check file guard) and [PR #24](https://github.com/zekusmaximus/client-portfolio/pull/24) (integer client ids) | Section 6's session items pass. Where the plan and the code differed, the code won: (1) the reset does not assume `client_revenues` is the only table that references `clients`: it lists every foreign key to `clients` from the catalog, counts each referencing table, and refuses, changing nothing, while any key is not `ON DELETE CASCADE`; production may still hold `V2__update_clients_schema.sql`'s `revenues` table, which cascades, is counted and is emptied; (2) without `--confirm` the script prints the keys and the numbers of accounts, people, clients, revenue rows and other referencing rows, and exits 1 so it never reads as a successful reset; (3) it locks `clients` against writes (`EXCLUSIVE`, reads still work, 10 s lock timeout) before its REPEATABLE READ snapshot, because without the lock a client saved during the reset would survive it; (4) added, section 6: **Check file** (`dryRun: true` on `/process-csv`), so the sheet can be made clean before the book is emptied; the API's refusal is identical in both modes, and the page words its own heading ("check it again") so a failed check does not suggest uploading into the old book; `dryRun` must be a boolean, so a string can never be read either way; (5) found while writing the runbook: the page totals only the reporting year (the header and the Dashboard's Total Revenue), so the spot check "the dashboard's total for each year" could not be done for earlier years; the import and Check file now return `summary.revenueTotals`, each year's total of the rows the import writes, and the page shows it beside each year; (6) found while writing the runbook: `deploy/backup/RESTORE.md` (a) downloads the latest successful run, which after the reset holds the new book, so runbook 7.4 step 8 says to use step 3's run id; (7) `deploy/backup/pg-backup.sh` accepts an empty `clients` table (it requires the three tables to exist and only `users` to be non-empty), so the nightly backup needs no change; (8) found after the merge, fixed in the follow-up: Netlify publishes the page within a minute of a merge while Render may still run the previous API, and that API ignores `dryRun`, so Check file imported the file and the page said "Nothing was written" (reproduced against `c27df68`: 12 clients became 20). `/api/health` now lists `features: ["check-file"]`, the page sends no check to an API without it, and it shows "Nothing was written" only when the answer carries `dryRun: true`; runbook 7.4 starts by confirming the feature on the live API; (9) found on the first real Check file on Render, after the reset (100 clients and 251 revenue rows removed): a 500, `column "client_id" is of type integer but expression is of type uuid`. Production's `clients` and `client_revenues` predate `init-db.sql` and have integer ids; the import had cast client ids to `uuid` since 2026-04-28 (the revenue upsert) and in Phase 2 (the bulk update), and CI only ever ran on `init-db.sql`'s uuid tables. The import now compares ids and years as text and writes revenue as the client form does (delete the file's `(client, year)` pairs, then insert), with no `ON CONFLICT`, which needs a unique key production's table may lack; `tests/import-db.test.mjs` runs on both table shapes, and fails 10 of 14 on the production shape without the fix. **Run on production 2026-09-25 (Jeff):** `reset-book` printed `Removed 100 clients and 251 revenue rows. Accounts: 7 and people: 7, unchanged.` (the count and the `--confirm` ran from one paste, which is why runbook 7.4 step 4 is now two blocks); after PR #24 the real book imported on gbacpod.com and the second chairs were brought up to date by hand in the app. Jeff's: section 6's open items |
| 3b | Retire contract status (P13) | merged | `claude/sleepy-gauss-ggi1ht`, [PR #23](https://github.com/zekusmaximus/client-portfolio/pull/23) | The handoff's session items pass: the gates, `tests/schema.test.mjs` and `tests/import-db.test.mjs` on PostgreSQL, the greps, end to end on a scratch database, and headless Chromium. Where the handoff and the code differed, the code won: (1) `status` is dropped from client responses in one place, `withPeopleFields` in `utils/people.cjs`, which `toApiClient` and both `models/clientModel.cjs` mappers already call, rather than in each of the three; (2) `src/ClientCard.jsx` is not rendered anywhere (nothing imports it): its status dot is removed as asked, but the client cards on screen are `ClientListView`'s, which is where the browser check applies; (3) removing the status card left `reportingYear` unused in `DashboardView` (only that card's title read it) and `Badge` unused in `PartnerDeepDive`; both are removed, as lint requires; (4) section 5's pending check for Jeff used `Contract Period` in its example file; the example now omits it (it would still work, the column being ignored). `/api/health` `features` gains no entry: this PR's `import-db` suite run against `origin/main`'s server imported and checked every file without `Contract Period` as before, listing one non-blocking "invalid contract period" issue per row; only the four P13 assertions failed, on a `status` the new page never reads. Jeff's: the four checks after deploy in the PR description |
| 4 | Who's carrying what (Partnership tab) | merged | `claude/determined-brahmagupta-kilyyn`, [PR #25](https://github.com/zekusmaximus/client-portfolio/pull/25) | Section 7's session items pass. Where the plan and the code differed, the code won: (1) the Scenarios workflow reads the store's `partners`/`fetchPartners` (built from the legacy text, with its "Unassigned" entry); they stay for Phase 5, which the plan gives the successor pickers, minus the capacity figure, and nothing else reads them; (2) the Dashboard's "Partnership Health %" was a fourth composite, built on the clients ÷ 15 capacity formula and an 85% overload threshold, and read 100% until the Partnership tab had been opened; it is replaced by a "Heaviest lead book" card from the same model; (3) the deep dive's invented revenue trend is replaced, not just removed, by the real revenue of the clients the person leads now, year by year, labelled as their current book's history; (4) of the three exports, the transition-plan CSV exported the redistribution modeler's reassignments and goes with it; the capacity CSV becomes everyone's load; the report is rebuilt from the model; all read `strategicValue`; (5) averages are over the active people in the role, those with no clients included, and never an inactive person; a role with one active person (the emeritus) shows "—", a lead-book ratio is blank for someone who cannot lead and leads nothing, and a small non-zero load shows "<0.1×", not "0.0×"; (6) the client list's relationship-type and risk filters also moved to `NativeSelect` (`ui/select` showed the raw value `all`), and the filters sit on a row under a full-width search; (7) `src/ClientCard.jsx` was imported nowhere and read the legacy team; deleted; (8) four of lint's seven expected `exhaustive-deps` warnings lived in the deleted components, so three remain |
| 5 | Departure engine and Scenarios rework | 5a PR open; 5b not started | 5a: `claude/adoring-cray-i5m29a`, [PR #26](https://github.com/zekusmaximus/client-portfolio/pull/26) | Two PRs. 5a (the effort share, the engine, Stage 1, the store): section 8's 5a session items pass. Where the plan and the code differed, the code won: (1) P10 amended by Jeff (2026-09-25): a second chair carries 20% of a client's effort; (2) section 8 says the second-chair candidates come "from the Phase 6 logic", which did not exist: the ranking is born here (`rankCandidates`, `secondChairPool`, `createLedger`, `areaIndex` in `src/utils/departure.js`) for Phase 6 to reuse; (3) "lighter load" counts what the scenario has already given each person: the affected clients are settled heaviest effort first, in a fixed order (so accepting a default never moves another), otherwise a departing book lands on whoever was lightest at the start; (4) practice-area fit is the candidate's lead book for a lead and every client they hold, in either seat, for a second chair; (5) second-chair candidates rank by second-chair load alone, as specified, so a partner with a heavy lead book and no seats ranks as light as an idle associate: a question for Jeff, and one comparator to change; (6) a client the departure touches whose lead on record is not an active partner, or whose kept second chair is inactive (only possible for pre-People data), gets candidates for that seat too, so P3 holds after on every client the scenario changes; (7) the retention estimate went from Stage 1 and, with it, the prompt's "Expected Retention Rate" line, which would otherwise have defaulted to an invented 80%; the prompt's "Departing Partners" became "Departing" with each person's role, since anyone can leave; (8) found and fixed: `POST /api/scenarios/transition-plan` required `client.id` to be a string, so on production's integer ids every AI plan has answered 400 since WP2; the check is now `checkPlanRequest` in `utils/transitionPlan.cjs`, tested; the store and Stage 3 also looked a plan's client up by `c.id === clientId` (a text key), which misses integer ids; (9) Stage 2's plans and the open stage moved onto the store's existing `transitionPlans` and `successionWorkflow` already in 5a (P11), since neither was read by anything; (10) the `successionUtils.js` fixes raise the transition complexity, and so the succession risk, of every client with a cadence or a High conflict risk: both factors were always 0 before. Store's `partners`/`fetchPartners` stay until 5b (Stage 2's pickers, Stage 3's assignees). 5b (Stage 2's seats, the roster in the prompt, Stage 3, the import sheet): section 8's 5b session items pass. Where the plan and the code differed, the code won: (1) a plan's lead and second chair are the scenario's choices over the engine's candidates, one source for Stage 1's loads, Stage 2's pickers and the sheet, rather than fields copied onto the plan; approving pins them; (2) the prompt asks for both seats on every affected client, naming the current holder when they stay (or None for the second chair), which covers "a second chair when the seat is empty" without the page guessing whether the AI will promote the second chair; recommendations are never applied until the partner asks; (3) the server refuses (400, with the names) rather than drops a roster name that is unknown, inactive, leaving or repeated, and uses the People list's names and roles; (4) the parser no longer invents a timeline (30 days by default, or the first "N days" anywhere in the answer): a plan has one only when its TIMELINE section names it, and Stage 3 shows "not set" otherwise; (5) the page depends on the roster, so `/api/health` lists `transition-plan-roster` and Stage 2 sends nothing to an API without it; (6) the sheet has no formula guard and no BOM, unlike the load CSV, because the import must read each client's name exactly, and it writes a kept second chair, because a blank cell clears the seat; (7) approving no longer needs an AI plan, since the key may be missing and the seats are the decision; (8) Stage 3's tasks are the plan's action items owned by the new lead, with no due date until someone sets one (the old code dated them weekly and assigned them to the AI's paragraph); its Gantt zoom became one bar per dated transition in a table of seats; (9) Check file's message for a file with no year columns now reads "no revenue years, so revenue is unchanged" |
| 6 | Associate split | not started | | |

Status values: `not started`, `in progress (date)`, `PR open`, `merged`, `deployed`, `verified on gbacpod.com`.

---

## 3. The import sheet

This is the format Phase 2 builds and the sheet Jeff assembles (with Cowork) for Phase 3. Save it as CSV (UTF-8); the page parses CSV in the browser. One header row, then one row per client.

| Column | Required | Values | Notes |
|---|---|---|---|
| `CLIENT` | yes | the client's name | as it should appear in the app; unique in the file |
| `2024 Contracts`, `2025 Contracts`, `2026 Contracts` | at least one | amounts: `72000`, `$72,000` or `$72,000.00`; blank or `0` for none | one column per year (D5). After the reset these columns are the only revenue history, so include every year the dashboard should show |
| `Lead` | yes | one active partner's name | exactly as on the People list (case does not matter). A file without this column (revenue only, or judgments only) leaves every client's people as they are; `Second Chair`, `Originator` and `Credit To Firm` need it |
| `Second Chair` | no | any active person other than the lead | blank for none |
| `Originator` | no | a person's name, or `Firm` | anyone on the People list, active or not; add a former colleague as an inactive person first if the sheet names them |
| `Credit To Firm` | no | `Y` or blank | `Y` when the originator's origination credit has ended; implied when `Originator` is `Firm`. Without this column, a person in `Originator` keeps the client's stored credit flag |
| `Stickiness` | no | `1` to `5` | 5 Personal bond (won't leave), 4 Strong, established, 3 Solid but transactional, 2 New / still shallow, 1 Cold (never met in person). Half of the strategic score |
| `Cadence` | no | `Daily`, `Weekly`, `Monthly`, `Quarterly`, `As-Needed` | how often the client is touched; drives effort |
| `Handful` | no | `Y` or blank | every interaction is heavy; effort × 1.5 |
| `Conflict Risk` | no | `Low`, `Medium`, `High` | subtracts 0, 1 or 3 from the score |
| `Practice Area` | no | one or more of `Healthcare`, `Municipal`, `Corporate`, `Energy`, `Financial`, `Education`, `Transportation`, `Environmental`, `Technology`, `Real Estate`, `Non-Profit`, `Other`, separated by `;` | |
| `Notes` | no | free text | |

A `Contract Period` column is ignored like any other header not listed here (P13): a sheet that still has it imports exactly as one without it, whatever its cells hold.

Header line and two example rows:

```csv
CLIENT,2024 Contracts,2025 Contracts,2026 Contracts,Lead,Second Chair,Originator,Credit To Firm,Stickiness,Cadence,Handful,Conflict Risk,Practice Area,Notes
Example Health Network,"$60,000","$66,000","$72,000",Kevin,Jay,Jay,Y,5,Weekly,,Low,Healthcare,
Example Energy Coalition,,"$40,000","$85,000",Paula,,Paula,,3,Monthly,Y,Medium,Energy;Environmental,Renewal talks in spring
```

Headers and values match regardless of case (`weekly`, `high`, `y`). A blank cell in a column the sheet has clears that field; a column the sheet lacks leaves it alone. Any problem refuses the whole file and the upload page lists each one by row, counting the header as row 1.

Without the judgment columns every client gets the same stickiness fallback and a Medium conflict penalty, so the strategic score ranks by revenue alone until partners fill them in one client at a time. If the sheet omits a judgment column, a later import that includes it fills it in.

---

## 4. Phase 1: People and second chair

**Expected outcomes**

- A People dialog (header button) lists everyone by role, adds a person, renames, changes a role, and deactivates or reactivates, refusing a change P5 forbids with the reason.
- The client form picks a lead from active partners, a second chair from everyone else who is active, and an originator with a "credit to the firm" toggle. The server refuses a client without an active partner lead, a second chair equal to the lead, or an unknown person, with a message on the field.
- Existing views keep working through the legacy fields (P6), lead-only as before. Until Phase 4, a second chair who leads nothing appears on the Partnership tab as a "partner" card with no clients, because that tab lists every name in `lobbyist_team`.

**Changes**

1. `init-db.sql`: `people` with the unique index on `lower(name)` and the seed (P1); `clients.lead_id`, `second_chair_id`, `originator_id` (`REFERENCES people(id)`, nullable) and `originator_is_firm BOOLEAN NOT NULL DEFAULT false`, each `ADD COLUMN IF NOT EXISTS`; `CHECK (second_chair_id IS NULL OR second_chair_id <> lead_id)` added by a guarded `DO` block. No backfill: the book is replaced in Phase 3, and until then a client keeps its legacy names until it is next saved.
2. `utils/people.cjs` (pure): the roles, the person and assignment validators, the P5 blockers, the SQL that joins a client's three people, and `withPeopleFields(row)`, which nests `lead`, `secondChair` and `originator` and fills the legacy fields (P6).
3. `routes/people.cjs`, mounted at `/api/people` behind `authenticateToken`: `GET /` (everyone, with how many clients each leads, seconds and originated), `POST /` `{ name, role }`, `PUT /:id` `{ name?, role?, active? }` (409 with the reason for a P5 violation or a duplicate name; a rename rewrites the legacy text in the same transaction). No delete. `sanitizeRequestBody` is not applied: names are validated against a strict pattern and React escapes them on render.
4. `data.cjs` `GET/POST/PUT /clients` and `models/clientModel.cjs` join the people and return `withPeopleFields`. `POST` and `PUT` take `lead_id`, `second_chair_id`, `originator_id` and `originator_is_firm`, validate them, answer 400 `{ success: false, error: 'Validation failed', details: [{ field, message }] }` (the shape the form already parses), and store the legacy text. The CSV import is unchanged until Phase 2.
5. Frontend: `people` in the store (loaded with the clients after sign-in, cleared on logout), `src/PeopleDialog.jsx`, `src/utils/people.js` (pure: candidates for each picker, labels), the form's "Team Assignment" section replaced, `src/constants.js` deleted.
6. `PRODUCT_BRIEF.md`: the "Who it's for" paragraph states P2.

**Tests**

- `tests/people.test.mjs`: the validators, the blockers, `withPeopleFields` for a new-model and a legacy row, and the frontend picker helpers.
- `tests/schema.test.mjs`: a new database has the seven people and the new columns; applying `init-db.sql` again adds nobody; a renamed person is not re-seeded; names are unique regardless of case; the check refuses a second chair equal to the lead; the foreign keys refuse an unknown person; a database built with the pre-plan `init-db.sql` migrates with its clients and revenue intact.

**Acceptance**

- [x] Gates green (lint 0 errors and the 7 expected warnings; `npm test` 89 of 89, and 99 of 99 with `SCHEMA_TEST_SERVER_URL` set; production build); `tests/schema.test.mjs` 14 of 14 on PostgreSQL 16 locally; `deploy/backup/selftest.sh` 14 passed with the new table. CI's `schema` job (PostgreSQL 18) runs on the PR.
- [x] End to end in the container, on a database built with the pre-plan `init-db.sql` holding a legacy client (lead Steve, team Steve and Fritz): the server migrated it at start; the API refused no lead, an emeritus lead, second chair = lead, an unknown originator, an inactive second chair, a duplicate name regardless of case and the reserved name `Firm`, deactivating a second chair, and moving or deactivating a partner who leads a client, each with its message; a rename rewrote the stored legacy text. In headless Chromium: the People dialog listed six partners and the emeritus, added an associate and showed the refusal for deactivating Kevin; the form refused a client without a lead, offered only partners as lead, created a client with Paula as lead, the associate as second chair and the emeritus as originator with credit to the firm, and saved the legacy client with a new lead after showing its old names. No console error from this change (React's pre-existing warning from `ClientListView` aside, status row note 5).
- [ ] Jeff, after deploy: the People dialog shows the six partners and Jay; the Render start-up log shows `Database tables initialized`.

---

## 5. Phase 2: Import reads people and judgments

**Outcome:** a sheet in the section 3 format imports leads, second chairs, originators and the judgment columns, or refuses with every bad row listed.

- `utils/csvImport.cjs` (pure): read the optional columns by header (case-insensitive, trimmed), parse `Y`, `1`–`5`, the cadence and conflict vocabularies and `;` lists; resolve names against the people list; return per-row errors. `processCSVData` carries the parsed fields; `validateClientData` merges the people errors into `issues`.
- `data.cjs` `/process-csv`: when any people error exists, answer 400 with the list and write nothing. Otherwise the insert and update passes set `lead_id`, `second_chair_id`, `originator_id`, `originator_is_firm` and the judgment columns for the columns present (P8), and the legacy text (P6). The current "preserve the existing value" logic stays for columns the file lacks.
- `DataUploadManager.jsx`: the expected-columns help lists section 3, with a "Download template" link to a static `public/client-book-template.csv`.
- Tests: parsing, name resolution, every refusal, a file without the people columns behaving exactly as today.

**Acceptance**

- [x] Gates green (lint 0 errors and the 7 expected warnings; `npm test` 119 of 119; production build, which ships `client-book-template.csv`); `tests/schema.test.mjs` 14 of 14 and `tests/import-db.test.mjs` 8 of 8 on PostgreSQL 16 locally. CI's `schema` job (PostgreSQL 18) runs both on the PR.
- [x] End to end in the container, on a scratch database with an account from `create-admin.cjs`: the section 3 example imported both clients with their leads, second chair, originators, credit to the firm, judgments and revenue; the People counts were Kevin 1 lead, Paula 1 lead and 1 originated, Jay 1 second chair and 1 originated; the stored legacy text was `Kevin` / `{Kevin,Jay}` / `Firm` and `Paula` / `{Paula}` / `Paula`. A file with an unknown lead, a Stickiness of 9 and a `CLIENT` repeated in another case was refused with all three listed (rows 2, 3 and 5) and the clients and revenue rows unchanged (checksums before and after). A file with only `CLIENT`, `Contract Period` and `2026 Contracts` updated 2026, kept 2024 and 2025, and left every client's people and judgments as they were. In headless Chromium: the help lists section 3, "Download template" saved a file equal to `public/client-book-template.csv`, the refusal rendered as a Row / Client / Problem table, a success named the columns it read, and a row without `CLIENT` showed its message. No console error from this change (the page's pre-existing 401s before sign-in and the intended 400s aside).
- [ ] Jeff, after deploy: the Data Upload page shows the new column list and "Download template"; a one-row test file naming a lead who is not on the People list (for example `CLIENT,Lead` / `Test Client,Nobody`) is refused with row 2 listed, and the client count in the header does not change. Do not import the template itself: its two example rows would be added to the book.

## 6. Phase 3: Fresh book

- `scripts/reset-book.cjs` (`npm run reset:book`, then `npm run reset:book -- --confirm`): without an argument it prints every foreign key to `clients` and the numbers of accounts, people, clients, revenue rows and the rows of any other table that references `clients`, deletes nothing and exits 1. With `--confirm`, one REPEATABLE READ transaction: lock `clients` against writes; refuse, changing nothing, while any foreign key to `clients` is not `ON DELETE CASCADE`; delete every client (revenue rows, and any legacy `revenues` rows, cascade); commit only if `clients`, `client_revenues` and every other referencing table are empty and the numbers of accounts and people are unchanged; print `Removed <n> clients and <r> revenue rows. Accounts: <a> and people: <p>, unchanged.` Accounts and people are untouched, so every People count is 0 afterwards and the P5 blocks lift. No API endpoint and no button: it runs from the Render Shell or a machine holding the External Database URL. The foreign-key check and the commit check are pure (`checkClientForeignKeys`, `checkBookReset` in `utils/schemaCheck.cjs`). Runbook: `deploy/README.md` 7.4.
- Added in Phase 3, **Check file**: `POST /api/data/process-csv` accepts `{ csvData, dryRun: true }` and runs the same checks and the same writes inside its transaction, then rolls back: the same 400 on any problem, or `{ success: true, dryRun: true, validation, summary }` with nothing written. The upload page has a **Check file** button beside Upload that shows the same refusal table, or `The file is ready to import: <n> clients; years ...; columns ... Nothing was written.` without the updated/new split, which before a reset is measured against the old book. Every import and check also returns each year's total (`summary.revenueTotals`), shown beside each year, for comparing with the sheet's column sums.
- Jeff: runbook 7.4, in order: assemble the sheet and put every name in it on the People list; Check file until it passes; run the backup by hand and keep that run's artifact beyond 90 days if the old book should outlive the retention (`deploy/backup/INSTALL.md` step 7); `reset-book` without and then with `--confirm`; import the sheet; spot-check; remove Steve's and Fritz's accounts if they have them (`check-schema`, then `delete-user`); add associates on the People dialog as they join.

**Acceptance**

- [x] Gates green (lint 0 errors and the 7 expected warnings; `npm test` 126 of 126, and 151 of 151 with `SCHEMA_TEST_SERVER_URL` set; production build); `tests/schema.test.mjs` 25 of 25 and `tests/import-db.test.mjs` 10 of 10 on PostgreSQL 16 locally. CI's `schema` job (PostgreSQL 18) runs both on the PR. Mutation checks: a dry run that commits, `db.cjs` required before the argument check, and the cascade refusal removed each fail the suites.
- [x] End to end in the container, on a scratch database with an account from `create-admin.cjs`, an associate and an inactive former partner on the People list, and a 12-client old book (2023 to 2025): Check file refused a bad sheet with its four problems listed (unknown second chair, emeritus lead, bad cadence, a client repeated in another case) and passed the good one, with the clients, revenue and people checksums unchanged both times; `reset-book` without an argument printed the key, 1 account, 9 people, 12 clients and 36 revenue rows, exited 1 and changed nothing; `--confirm` printed `Removed 12 clients and 36 revenue rows. Accounts: 1 and people: 9, unchanged.` with the users and people rows byte-identical and every People count 0; the 8-client sheet then imported with year totals $135,000, $235,000 and $290,000, equal to the sheet's column sums. Also: a legacy V2 `revenues` table was counted and emptied through its cascade, a foreign key without a cascade was refused by name, and a write held open on `clients` made `--confirm` refuse after 10 s with nothing deleted.
- [x] In headless Chromium against the scratch API: Check file on the bad sheet showed the refusal table under `The file is not ready to import: it has 4 problems. ... Nothing was written.` and on the good sheet `The file is ready to import: 8 clients; years 2024 ($135,000), 2025 ($235,000), 2026 ($290,000); columns Lead, ... Nothing was written.`, both requests carrying `dryRun: true` and neither changing the database; after a reset, Check file and then Upload imported the sheet, the header showed `8 clients loaded`, and the People dialog's counts matched the sheet. No console error from this change (the page's pre-existing 401s before sign-in and the intended 400 aside).
- [ ] Jeff, after deploy: <https://client-portfolio-backend.onrender.com/api/health> answers with `"features":["check-file"]` (Render runs the Phase 3 API).
- [ ] Jeff: the sheet in the section 3 format, with every associate it names and every former colleague it names as an originator on the People list (the former colleagues inactive).
- [ ] Jeff: Check file passes on gbacpod.com, and each year's total equals the sheet's column sum.
- [ ] Jeff: the backup run by hand is green, its artifact is downloaded and kept outside GitHub if the old book should outlive 90 days; record the run id here.
- [x] Jeff, 2026-09-25: `reset-book`, one key line (`client_revenues`, `ok`; production has no V2 `revenues` table), 7 accounts, 7 people, 100 clients, 251 revenue rows (the counts of the 2026-09-24 backup), then `--confirm`: `Removed 100 clients and 251 revenue rows. Accounts: 7 and people: 7, unchanged.`
- [x] Jeff, 2026-09-25: the sheet imported on gbacpod.com, after PR #24 (the first Check file answered 500 on production's integer client ids); second chairs then brought up to date by hand.
- [ ] Jeff: the header's total equals the latest year's column sum; a few clients' leads, second chairs and revenue checked (the client list now shows both chairs on each card, Phase 4); the People dialog's counts match the sheet.
- [ ] Jeff: Steve's and Fritz's accounts removed if they had them (`check-schema` ends `OK`, then `delete-user`).
- [ ] Jeff: the next nightly backup is green with the new book's counts.

## 7. Phase 4: Who's carrying what

The Partnership tab rebuilt from the people and the clients, by role: partners' lead books (clients, revenue in the reporting year, effort) and everyone's second-chair load, each compared to the role's average (P10). Removes the invented data (the deep dive's revenue trend at × 0.8 and × 0.9, the `Math.random` workload chart, the fixed "+11.1%"), the "Unassigned" pseudo-partner, the three capacity formulas, and the tab's own redistribution modeler (departures move to Scenarios). Fixes the `strategic_value` reads (the API sends `strategicValue`; five components and the exports read the wrong name, so "high-value clients at risk" is always 0 and the exports show N/A). The client list filters by lead or second chair and shows both, and stops calling `fetchPartners()` inside `useMemo` (a store update during render; React warns).

**As built**

- `src/utils/load.js` (pure): `partnershipModel(people, clients, revenueOf)` gives each person's lead book and second-chair load (clients, reporting-year revenue, effort from the API's `effort`), the averages over the active people in each role, and each figure's ratio to its average; plus `heaviestLeadBook`, `bookYears`, `revenueByYear`, `practiceAreaBreakdown` and the formatters. Phase 6 reuses it.
- The tab (`src/PartnershipAnalytics.jsx`, `components/LoadTable.jsx`, `components/PersonLoadSheet.jsx`): totals, a notice listing any client without a lead, the lead books (heaviest revenue first), the second-chair load by role, and, on a name, that person's clients in both chairs with the other chair named, their current book's revenue by year and its practice areas. Export: a printable report and everyone's load as CSV (`src/utils/partnershipExports.js`, pure builders).
- Removed: the redistribution modeler, the post-transition grid, the client-flow view, the scenario comparison, the transition checklist, the old metrics, grid, card and deep dive, `src/ClientCard.jsx` (unused), and the store's departing flags, redistribution and custom assignments, health and analytics getters. The store's `partners`/`fetchPartners` stay for Scenarios until Phase 5, without the capacity figure.
- The Dashboard's "Partnership Health %" became "Heaviest lead book": the partner leading the most revenue and that against the partners' average.
- The client list shows each client's lead and second chair, filters by either (with "No lead" and "No second chair"), and searches their names.

**Acceptance**

- [x] Gates green (lint 0 errors and 3 expected warnings; `npm test` 141 of 141, and 184 of 184 with `SCHEMA_TEST_SERVER_URL` set; production build); `tests/schema.test.mjs` and `tests/import-db.test.mjs` 53 of 53 on PostgreSQL 16 locally. CI's `schema` job (PostgreSQL 18) runs both on the PR.
- [x] End to end in the container, on a database with production's older tables (integer ids), a 24-client book imported from a section 3 sheet, two associates and an inactive former partner. In headless Chromium, every figure matched SQL on the same database: the lead books Kevin 7 / $945,000, Paula 5 / $562,000, Mike 4 / $523,000, Jeff 4 / $272,000, Joe 3 / $228,000, Brendan 1 / $73,000, in that order; the second chairs Anna 6, Jay 4, Ben 3 and the partners' seats; 5 clients without a second chair. Kevin's panel listed his 7 and 2 clients and his book's revenue by year. The CSV downloaded with a BOM and one row per person; the report opened in a window with no "N/A" and no capacity figure. The Dashboard card named Kevin at 2.2× the partners' average. The client list filtered to Kevin's 7, to the 5 without a second chair, and found Anna's 6 by search. Scenarios still listed its partners. No console error or warning (the old `fetchPartners`-in-render warning is gone).
- [ ] Jeff, after deploy: the Partnership tab's lead books and second-chair counts agree with the People dialog's counts; the Dashboard card names the partner with the largest lead book; the client list's lead and second-chair filters narrow the list as expected; Export gives the report and the CSV.

## 8. Phase 5: Departure engine and Scenarios rework

- A pure, tested departure engine: for one or more departing people, each client a departing partner leads needs a new partner lead, with suggested candidates (the second chair first when a partner, then practice-area fit, then lighter lead load); each seat a departing second chair held becomes empty, with candidates from the Phase 6 logic. Output: the clients needing a decision and every person's load before and after.
- Stage 1 uses it and lists every active person, by role, as departable. Stage 2's plan holds a new lead and a new second chair, checked against P3, instead of one `successorPartner` string (today the AI's paragraph is copied into that field). The transition-plan prompt receives the roster with roles and loads and must name its recommendation from it; the parser reads that name separately. Stage 3's mock data ("ClientCorp", fixed 95% retention and 45 days) and the retention estimate built on the retired `relationshipStrength` are removed. Scenario state moves into the store (P11).
- Fixes found in the trace: `successionUtils.js` reads `communication_frequency` (the column is `interaction_frequency`) and `parseFloat`s the conflict label; the successor pickers list the departing people and "Unassigned".

**P10 amended (Jeff, 2026-09-25), PR 5a's first commit.** A second chair's effort on a client counts as 20% of the client's effort for load; the lead carries it all. Client counts and revenue stay whole for both chairs; each client's own effort (`getEffort`, sent as `client.effort`) and the strategic score do not change. One constant, `SECOND_CHAIR_EFFORT_SHARE` in `src/utils/load.js`: the Partnership tab, its report and CSV, the person panel, the departure engine and Phase 6 read it. The server does not need it; if it ever does, mirror it in a CommonJS module with a parity test.

**As built (PR 5a)**

- `src/utils/departure.js` (pure): `departureModel({ people, clients, departingIds, revenueOf, choices })` gives the clients that need a decision, each seat's candidates in order and who holds it after, the clients no partner can lead (`unresolved`), and `partnershipModel` before and after (the departing people inactive after, so out of the role averages) with each person's two rows paired by role. Lead candidates: active partners who are staying; the client's second chair first when one of them, then the most of the client's practice areas in their lead book, then the lighter lead load (effort, revenue, name). Second-chair candidates: everyone active and staying but the lead, by the most shared areas across every client they hold, then the lighter second-chair load (effort with the share, revenue, name). A seat is emptied when its holder leaves or is promoted to lead. Loads count the scenario's earlier seats (the clients settled heaviest effort first). Choices (`{ [clientId]: { leadId, secondChairId } }`, a `secondChairId` of null for an empty seat) are applied unless `assignmentProblems` gives a reason, in which case the default stands and the reason is kept for Stage 2. No candidate leaves the seat empty with `noCandidate`.
- Stage 1 (`ImpactAnalysisWorkbench.jsx`): "Who is leaving" lists every active person by role with their seats; the tiles (clients affected, their revenue, new leads, seats to fill, high succession risk); an alert naming any client no partner can lead; the affected clients with each seat now and its proposal, the reason and the next candidates; everyone's load before and after in one table by role; the practice-area and risk views, the heat map and the risk categories as before. The retention probability and projected loss tiles are gone.
- `SuccessionScenario.tsx` runs the engine once and passes it down; the stage, the departing ids and the choices are the store's `successionWorkflow`, Stage 2's plans the store's `transitionPlans` (P11), cleared on logout and never persisted.
- `successionUtils.js`: the cadence from `interaction_frequency` and High conflict from the label; its imports carry `.js` so tests can load it.
- `checkPlanRequest` accepts production's integer client ids; the prompt states no retention estimate.

**Acceptance (PR 5a)**

- [x] Gates green (lint 0 errors and the 3 expected warnings; `npm test` 166 of 166, and the database suites below; production build).
- [x] `tests/schema.test.mjs` and `tests/import-db.test.mjs` on PostgreSQL 16 locally (this PR changes no SQL); CI's `schema` job (PostgreSQL 18) runs both on the PR.
- [x] End to end in the container, on a database with production's older tables (integer ids), a 24-client book imported from a section 3 sheet, two associates and an inactive former partner. In headless Chromium, Stage 1 listed the six partners, Jay and the two associates (not Steve, not "Unassigned") with their seats; Kevin and Anna marked as leaving gave 15 affected clients, $938,000, 6 new leads and 13 seats to fill (Anna's 7, Kevin's 4, and the two second chairs promoted to lead); every proposal named a staying person with its reason; every figure in the before-and-after table matched SQL on the same database, before and with the proposals applied, and SQL found no P3 violation in the proposed book. The marks and the table were unchanged after switching to the Dashboard and back, and Stage 2 was still open after switching to Partnership and back. Stage 2's "Generate AI Plans" on an integer-id client reached the AI path and showed "AI is not configured on the server (missing API key)." (it answered 400 before this PR). No console error from this change (the page's pre-existing 401s before sign-in aside).
- [ ] Jeff, after deploy: on Scenarios, mark a partner and an associate as leaving; the affected clients and the loads before and after agree with the Partnership tab (before) and with the proposals (after); switching tabs keeps the marks.

**As built (PR 5b)**

- Stage 2 (`ClientReviewInterface.jsx`): each affected client's plan holds a new lead and a new second chair, the scenario's choices over the engine's candidates (`successionWorkflow.choices`), in place of the `successorPartner` string. The pickers are `NativeSelect`s listing only the engine's candidates, in order and with the reason (never someone leaving, never "Unassigned", never the lead as second chair); a pick P3 or the departures refuse shows its reason under the picker and the proposal stands; "Back to the proposal" forgets the client's picks. The bulk bar assigns a new lead to the selected clients that need one, sets a timeline and approves. Approving pins the client's seats as choices (`approveTransitionPlans`); a client no partner can lead, or with a refused pick, cannot be approved, and says why. Approving needs no AI plan.
- The transition-plan request (`planRequest` in `src/utils/transitionPlans.js`) carries the roster: the active people staying, with their lead and second-chair loads from the departure model's `before` (the Partnership tab's figures). `checkRoster` in `utils/transitionPlan.cjs` refuses, with the names, a roster of more than 50, a name not on the People list, an inactive person, someone leaving or a name given twice, before the model is called; the names and roles sent to the model are the People list's. The prompt lists the roster, marks the current lead and second chair "(leaving)", and asks for a `RECOMMENDED LEAD` (a partner) and a `RECOMMENDED SECOND CHAIR` (or None) by name on each section's first line. `resolveRecommendation` resolves each against the same roster: a name off it, two names, a non-partner lead or a second chair who is the lead give nobody, with the reason. The page shows the recommendation and its reasoning; "Use these picks" makes them choices, which the engine checks again. `/api/health` lists `transition-plan-roster`, and the page sends no plan request to an API without it.
- The accepted plan exports as an import sheet (`buildTransitionSheet`): `CLIENT,Lead,Second Chair`, one row per approved client, the seats as the engine applies them, a kept second chair written out (a blank cell clears the seat), quoted but with no formula guard and no BOM so the import reads each name exactly. `TransitionSheetPanel.jsx`, on Stages 2 and 3, exports it, lists approved clients it cannot include and why, and says who holds no seat once it is uploaded (and so can be deactivated, P5) and who still does. The partner applies it with Data Upload's Check file, then Upload; no new write endpoint. Check file on a file without year columns now says "no revenue years, so revenue is unchanged" instead of "years none".
- Stage 3 (`TransitionPlanManager.jsx`) and the store's execution state: the approved clients with their new lead and second chair (and who held each seat that changes hands), the date the plan entered Stage 3, the plan's timeline and end date only when the plan has one, a status per transition, the plan's action items as tasks owned by the new lead with no due date until someone sets one, and a communication log, all in the store (`startExecution` keeps what was recorded when Stage 3 is reopened). Removed: the "ClientCorp" and "3 tasks due" alerts, `retentionRate: 95`, the fixed 45 days and the trend figures, the invented weekly due dates, and the AI's paragraph as a task's assignee.
- The store's `partners` and `fetchPartners` (the legacy text with "Unassigned") are gone; so are the unused `executionMetrics`, `executionAlerts` and `initializeTransitionsFromPlans`. A "Start over" button beside the stepper (after a confirmation) clears who is leaving, the picks, the plans and Stage 3, for the next scenario once a sheet has been applied.

**Acceptance (PR 5b)**

- [x] Gates green (lint 0 errors and the 3 expected warnings; `npm test` 176 of 176; production build); `tests/schema.test.mjs` and `tests/import-db.test.mjs` 57 of 57 on PostgreSQL 16 locally, including the route's roster check on both table shapes and a transition sheet changing the two seats and nothing else. CI's `schema` job (PostgreSQL 18) runs both on the PR. Mutation checks: accepting an unknown or leaving roster name, a non-partner lead, two names, or a name outside the section's first line each fails the suite.
- [x] End to end in the container, continuing on the 5a database (production's older tables, 24 clients). Kevin and Anna leaving: the six lead pickers offered only Brendan, Jeff, Joe, Mike and Paula; the fifteen second-chair pickers only those five, Jay, Ben and None; nobody leaving, inactive or "Unassigned". On Pharma Alliance, a second chair of Jeff and then Jeff as lead showed "Your pick was not applied: Jeff is the lead; the second chair must be someone else." and blocked approval until Ben was picked. Generate AI Plans without a key listed "AI is not configured on the server (missing API key)."; the request carried an integer client id and the roster of seven staying people with their loads. All 15 approved; the exported sheet listed them with their seats; Stage 3 showed no mock data and survived a tab switch; deactivating Kevin before the upload answered 409. Check file on the sheet wrote nothing; Upload changed only the seats (every other client column, every revenue row and the People list byte-identical, by md5), left Kevin and Anna with no seat, and every person's load in SQL then equalled Stage 1's "after" column for these picks. Kevin and Anna were then deactivated in the People dialog and left Stage 1's list. With `/api/scenarios/transition-plan` answered by the server's own parser (Playwright routing): an API without `transition-plan-roster` got no request and the page said why; a lead recommended off the roster showed "nobody (The recommendation names nobody on the roster.)" and "Use these picks" applied only the valid second chair; Stage 3 showed the plan's 45-day timeline and its action items owned by the new leads. No console error from this change (the pre-existing 401s before sign-in and the intended 503 and 409 aside).
- [ ] Jeff, after deploy: `/api/health` lists `transition-plan-roster`; on Scenarios, a plan for one client names people from the roster (with `ANTHROPIC_API_KEY` set); an exported sheet passes Check file on Data Upload. Do not upload a sheet from a scenario that is not meant: it changes the leads and second chairs it lists.

## 9. Phase 6: Associate split

A view of each associate's second-chair load against the associate average, the clients with no second chair, and a proposed assignment for them (practice-area fit, then lighter load, respecting P3). A partner accepts or edits each proposal; accepting writes `second_chair_id` (P9). Reuses Phase 4's load helpers and Phase 5's candidate logic.
